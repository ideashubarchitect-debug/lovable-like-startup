# KloudBean Django - Dependencies
# Pin versions for reproducible builds. Include all direct + key transitive deps so build has every package.

Django==4.2.11
gunicorn==21.0.0
asgiref>=3.7.2,<4
sqlparse>=0.4.3
tzdata
PyMySQL>=1.1.0
