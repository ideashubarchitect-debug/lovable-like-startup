# KloudBean production environment
# Copy to .env or set these in KloudBean → Application → Environment variables.
# Do not commit .env with real values.

# Django
DJANGO_SECRET_KEY=change-me-to-a-strong-secret-min-50-chars
DJANGO_DEBUG=False
ALLOWED_HOSTS=django-642200409.kloudbeansite.com,.kloudbeansite.com,localhost,127.0.0.1

# Database (KloudBean MySQL) – replace with your DB credentials from KloudBean panel
DB_ENGINE=mysql
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=3306

# Optional: single URL if KloudBean provides it
# DATABASE_URL=mysql://user:password@host:3306/dbname
