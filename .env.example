# KloudBean production environment
# Match KloudBean Django docs: https://support.kloudbean.com/docs/application-deployment/deploying-django
# Create as .env in django-src (File Manager or shell). Do not commit .env with real values.

# Application URL (as in KloudBean docs)
APP_URL=https://django-642200409.kloudbeansite.com

# Django
DJANGO_SECRET_KEY=change-me-to-a-strong-secret-min-50-chars
# Set to 1 or true to see detailed 500 errors; set to 0 or false in production.
DJANGO_DEBUG=0
ALLOWED_HOSTS=django-642200409.kloudbeansite.com,.kloudbeansite.com,localhost,127.0.0.1

# Database (KloudBean uses DB_USERNAME in docs – we support both DB_USERNAME and DB_USER)
DB_ENGINE=mysql
DB_HOST=localhost
DB_NAME=your_db_name
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password
DB_PORT=3306
